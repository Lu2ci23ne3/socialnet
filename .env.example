DEBUG=True
SECRET_KEY=troque-esta-chave-em-producao-por-uma-aleatoria
ALLOWED_HOSTS=localhost,127.0.0.1

# Deixe em branco para usar SQLite (padrão, ótimo para desenvolvimento)
# Para PostgreSQL em produção, preencha:
DB_ENGINE=
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=

# Redis é opcional em dev (usa camada em memória se não configurado).
# Necessário em produção para o chat em tempo real funcionar com múltiplos processos/servidores.
REDIS_URL=
